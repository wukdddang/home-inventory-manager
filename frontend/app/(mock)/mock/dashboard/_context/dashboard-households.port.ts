/**
 * mock 경로 호환용 re-export.
 * 포트 정의는 (current)/dashboard/_context에서 관리합니다.
 */
export type {
  CreateInvitationParams,
  DashboardHouseholdsPort,
} from "@/app/(current)/dashboard/_context/dashboard-households.port";
