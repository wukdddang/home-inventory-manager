# TODO — 2026-04-05

배포 전 점검에서 도출된 후속 작업 목록.
완료된 항목은 `[x]`로 체크하고, 모든 항목이 완료되면 이 파일을 삭제한다.

---

## 보안

- [ ] **CORS origin 제한** — `backend/src/main.ts`의 `app.enableCors()`에 프론트엔드 도메인을 명시적으로 지정한다.
  - 현재: `app.enableCors()` (모든 origin 허용)
  - 변경: `app.enableCors({ origin: process.env.APP_URL })` 또는 Vercel 도메인 직접 지정
  - 시점: 프론트엔드 도메인 주소 확정 후

## 인프라

- [ ] **DB 마이그레이션 전략 수립** — 프로덕션에서 `synchronize: false`이므로 스키마 변경 시 마이그레이션 방법을 정한다.
  - 옵션 A: 최초 1회 `synchronize: true`로 기동 후 끄기
  - 옵션 B: TypeORM CLI `migration:generate` + `migration:run` 파이프라인 구축
  - 위치: `backend/src/common/database/database.module.ts`
